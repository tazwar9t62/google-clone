import React, { useEffect, useState } from "react";

const CONTEXT_key = "4aec33930ea9a5e7f";
const useGoogleSearch = (term) => {
  const [data, setData] = useState(null);
  useEffect(() => {}, [term]);
};

export default useGoogleSearch;
